## finans.PAYABLE_READ_MODEL_V1 — 2026-09-07

**Ödeme Planı PAYABLE Read-Model V1 (Candidate)**

Cariler sekmesi soğuk açılışı 18.3 saniyeden <800ms'ye indirgendi.
Korgün `fetch_supplier_balances_bundle()` web request yolundan çıkarıldı.

### Yapılan değişiklikler
- `app/modules/finans/read_model/` yeni paket (8 modül)
- `routes.py`: cariler sekmesi → snapshot okuma (inline Korgün=0)
- `odeme_plani.html`: snapshot durum banner'ları (5 durum)

### Güvenlik
- Canonical `app/mock_data.db`: DOKUNULMADI
- Production runtime DB: OLUŞTURULMADI (deploy adımı)
- Scheduled Task: KAYITLI DEĞİL (deploy adımı 6)
- Main branch: DOKUNULMADI (worktree candidate)

### Test
31 PASS · 1 SKIP · 0 FAIL — tüm temp SQLite, canonical DB dokunulmadı
